(ns user
  (:require [clojure.tools.namespace.repl :refer (refresh)]
            [clojure.walk :as walk]
            [clojure.pprint :refer [pprint]]))
            ;; [cats.core :as m]
            ;; [cats.builtin :as builtins]
            ;; [cats.monad.maybe :as maybe]
            ;; [cats.monad.either :as either]))
